---
phase: 01-working-app
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/src/stores/settingsStore.ts
  - frontend/src/components/Onboarding.tsx
  - frontend/src/components/SettingsDialog.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "On first launch (no saved settings), user sees an onboarding screen with Hatchet API key and Redis URL fields"
    - "Validation errors appear inline under the specific field that failed"
    - "After saving valid settings, they persist across app restarts via tauri-plugin-store"
    - "Settings are accessible later via a SettingsDialog component (used by header gear icon and tray menu)"
  artifacts:
    - path: "frontend/src/stores/settingsStore.ts"
      provides: "Settings load/save/validate functions using tauri-plugin-store"
      exports: ["loadSettings", "saveSettings", "hasSettings"]
      min_lines: 30
    - path: "frontend/src/components/Onboarding.tsx"
      provides: "First-launch onboarding form with two fields and validation"
      min_lines: 60
    - path: "frontend/src/components/SettingsDialog.tsx"
      provides: "Reusable settings dialog for editing credentials after onboarding"
      min_lines: 50
  key_links:
    - from: "frontend/src/stores/settingsStore.ts"
      to: "@tauri-apps/plugin-store"
      via: "load('settings.json') and store.get/set"
      pattern: "plugin-store"
    - from: "frontend/src/components/Onboarding.tsx"
      to: "frontend/src/stores/settingsStore.ts"
      via: "saveSettings import"
      pattern: "saveSettings"
---

<objective>
Build the settings persistence layer and onboarding UI so the app can collect and store Hatchet API key and Redis URL on first launch.

Purpose: The sidecar cannot connect to external services without credentials. This plan creates the credential entry flow and persistence layer that the startup sequence (Plan 04) gates on.

Output: A settings store module, a first-launch Onboarding component, and a reusable SettingsDialog for later access.
</objective>

<execution_context>
@/Users/yedidyakfir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yedidyakfir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-working-app/01-CONTEXT.md
@.planning/phases/01-working-app/01-RESEARCH.md
@.planning/phases/01-working-app/01-01-SUMMARY.md

@frontend/src/App.tsx
@frontend/src/components/ui/

<interfaces>
<!-- Existing shadcn/ui components available -->
Dialog, Form, Input, Button, Label, Alert from @/components/ui/*
sonner Toast from @/components/ui/sonner

<!-- tauri-plugin-store v2 API -->
import { load } from '@tauri-apps/plugin-store';
const store = await load('settings.json');
await store.get<string>('key');
await store.set('key', value);
await store.save();
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings store module with tauri-plugin-store</name>
  <files>frontend/src/stores/settingsStore.ts</files>
  <action>
Create `frontend/src/stores/settingsStore.ts` with the following:

1. **Types:**
   ```typescript
   export interface AppSettings {
     hatchetApiKey: string;
     redisUrl: string;
   }
   ```

2. **`loadSettings(): Promise<AppSettings | null>`:**
   - Call `load('settings.json')` from `@tauri-apps/plugin-store`
   - Get `hatchetApiKey` and `redisUrl` from store
   - If both exist and are non-empty strings, return `{ hatchetApiKey, redisUrl }`
   - If either is missing/empty, return `null` (triggers onboarding)
   - Wrap in try/catch — if store fails to load (e.g., running in browser dev mode without Tauri), return `null`

3. **`saveSettings(settings: AppSettings): Promise<void>`:**
   - Load store via `load('settings.json')`
   - `store.set('hatchetApiKey', settings.hatchetApiKey)`
   - `store.set('redisUrl', settings.redisUrl)`
   - `store.save()` — explicit save for durability (per research: auto-save only fires on clean exit)

4. **`hasSettings(): Promise<boolean>`:**
   - Convenience wrapper: calls `loadSettings()`, returns `result !== null`

5. **Environment detection:**
   - Add a helper `isTauriEnvironment(): boolean` that checks `window.__TAURI_INTERNALS__` existence
   - When NOT in Tauri (browser dev), use `localStorage` as fallback so the onboarding flow can be tested without Tauri running
   - Keep the fallback simple: `localStorage.getItem/setItem` with JSON serialization

Export all functions and the `AppSettings` type.
  </action>
  <verify>
    <automated>cd /Users/yedidyakfir/Documents/Research/mage-voyence/frontend && npx tsc --noEmit src/stores/settingsStore.ts 2>&1 | tail -5</automated>
  </verify>
  <done>settingsStore.ts exports loadSettings, saveSettings, hasSettings, AppSettings. Uses tauri-plugin-store in Tauri environment, localStorage fallback in browser. Explicit store.save() after writes.</done>
</task>

<task type="auto">
  <name>Task 2: Build Onboarding and SettingsDialog components</name>
  <files>frontend/src/components/Onboarding.tsx, frontend/src/components/SettingsDialog.tsx</files>
  <action>
**Onboarding.tsx:**

Create a full-screen onboarding component with:

1. **Layout:** Centered card on a branded background. Use shadcn Card component. App name "Mageflow Viewer" as heading. Subtitle: "Connect to your infrastructure to get started."

2. **Form fields (using react-hook-form + zod):**
   - "Hatchet API Key" — text input, required, non-empty validation
   - "Redis URL" — text input, required, must start with `redis://` or `rediss://`, default placeholder `redis://localhost:6379`

3. **Validation:**
   - Client-side: zod schema validates format before submission
   - Server-side: On submit, call a validation endpoint or attempt connection. For now, attempt to save settings and signal the parent to restart the sidecar. The actual service validation happens when the sidecar starts and the health check runs.
   - On validation failure: show inline error under the specific field using react-hook-form's `errors` object (per user decision: "inline error under the specific field that failed")

4. **Submit handler:**
   - Call `saveSettings({ hatchetApiKey, redisUrl })` from settingsStore
   - On success: call `onComplete()` callback prop to signal the parent (App.tsx) to proceed to sidecar startup

5. **Props:** `{ onComplete: () => void }`

**SettingsDialog.tsx:**

Create a reusable dialog for editing settings after onboarding:

1. **Uses shadcn Dialog component** with controlled open state via props

2. **Props:**
   ```typescript
   interface SettingsDialogProps {
     open: boolean;
     onOpenChange: (open: boolean) => void;
   }
   ```

3. **On open:** Load current settings via `loadSettings()` and populate the form

4. **Same form fields and validation as Onboarding** (extract shared form logic into a `SettingsForm` sub-component or shared hook to avoid duplication)

5. **Submit handler:**
   - Save settings via `saveSettings()`
   - Call `invoke('restart_sidecar')` from `@tauri-apps/api/core` to restart with new credentials
   - Show success toast via sonner
   - Close dialog

6. **Cancel button** closes without saving

Both components use Tailwind CSS for styling, consistent with the existing shadcn/ui design system.
  </action>
  <verify>
    <automated>cd /Users/yedidyakfir/Documents/Research/mage-voyence/frontend && npx tsc --noEmit src/components/Onboarding.tsx src/components/SettingsDialog.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>Onboarding.tsx renders a centered card with Hatchet API key and Redis URL fields, zod validation, inline errors, and calls onComplete on success. SettingsDialog.tsx wraps the same form in a Dialog, loads existing settings, saves changes, and triggers sidecar restart. Both use react-hook-form + zod + shadcn/ui components.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all new files
- Onboarding has two form fields with validation
- SettingsDialog has open/onOpenChange props
- settingsStore has loadSettings/saveSettings/hasSettings exports
- Settings use tauri-plugin-store (not localStorage) when in Tauri environment
</verification>

<success_criteria>
- Settings store persists hatchetApiKey and redisUrl via tauri-plugin-store
- Onboarding screen collects both fields with inline validation errors
- SettingsDialog allows editing settings and triggers sidecar restart
- Settings persist across app restarts (store.save() called explicitly)
</success_criteria>

<output>
After completion, create `.planning/phases/01-working-app/01-03-SUMMARY.md`
</output>
